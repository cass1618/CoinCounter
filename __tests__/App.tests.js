import calculateChange from './../CoinCounter.js';

describe('calculateChange', () => {
TextDecoderStream('it should return the amount of quarters, dimes, nickels, and pennies into a string', () => {
  
})
})